import React from "react";
import { AppBar, Toolbar, Typography } from "material-ui";

export default props => (
  <AppBar position="static">
    <Toolbar>
      <Typography varient="Display4" color="inherit">
        CS240 Project
      </Typography>
    </Toolbar>
  </AppBar>
);
